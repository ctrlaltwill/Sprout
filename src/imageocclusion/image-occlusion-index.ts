/**
 * @file src/imageocclusion/image-occlusion-index.ts
 * @summary Barrel re-export file for the Image Occlusion module. Re-exports ImageOcclusionEditorModal from image-mask-renderer so that consumers can use a single canonical import path via this index file.
 *
 * @exports
 *   - ImageOcclusionEditorModal — re-exported modal class for the IO mask editor
 */

export { ImageOcclusionEditorModal } from "./image-mask-renderer";
